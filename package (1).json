{
  "name": "scratchlearn-jwt-db-monorepo",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "npm-run-all -p dev:server dev:client",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}